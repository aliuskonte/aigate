services:
  postgres:
    image: postgres:16
    container_name: aigate-postgres
    environment:
      POSTGRES_DB: aigate
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d aigate"]
      interval: 5s
      timeout: 3s
      retries: 20

  redis:
    image: redis:7
    container_name: aigate-redis
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

  aigate:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: aigate-app
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      AIGATE_ENV: prod
      AIGATE_LOG_LEVEL: WARNING
      DATABASE_URL: postgresql+asyncpg://postgres:postgres@postgres:5432/aigate
      REDIS_URL: redis://redis:6379/0
      QWEN_API_KEY: ${QWEN_API_KEY}
      QWEN_BASE_URL: ${QWEN_BASE_URL:-https://dashscope-us.aliyuncs.com/compatible-mode/v1}
    ports:
      - "8000:8000"
    restart: unless-stopped
    networks:
      - default
      - monitoring

networks:
  monitoring:
    name: aigate_monitoring

volumes:
  postgres_data:
